# Copy to .env and fill in.

# Google Maps JavaScript API key, used by the clinic map on the Nearby care
# page. Restrict it by HTTP referrer (your domain + localhost) in the Google
# Cloud Console — Maps JS keys are exposed in the browser bundle by design.
VITE_GOOGLE_MAPS_API_KEY=
