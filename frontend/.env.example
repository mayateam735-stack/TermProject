# Copy to .env and fill in (optional).
#
# The clinic map on the Nearby care page uses free OpenStreetMap tiles via
# Leaflet — no API key or billing required, so there are no frontend env vars
# to set for it. This file is kept as a placeholder for any future VITE_* vars.
